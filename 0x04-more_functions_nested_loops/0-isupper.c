#include "main.h"

/**
 * _isupper - check letter
 * @x: the number to be checked
 * Return: 1 for upper or 0 for any other
 */

int _supper(int x)
{
	if (x >= 65 && x<= 90)
	{
	return (1);
	}
	return (0);
}
