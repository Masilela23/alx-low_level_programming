#include "main.h"

/**
 * _isupper - check letter
 * @c: the number to be checked
 * Return: 1 for upper or 0 for any other
 */

int _supper(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}
